name: Deploy Urion Project

on:
  push:
    branches: ["**"]

permissions:
  contents: write
  packages: write
  actions: read

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: 📂 Repo-ni ko'chirib olish
        uses: actions/checkout@v4

      - name: 🟢 Node.js o'rnatish
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Kutubxonalarni o'rnatish
        run: npm ci

      - name: 🏗️ Loyihani Build qilish
        run: npm run build

      - name: 🚀 Serverga Deploy qilish
        run: |
          # Urion uchun target papka
          TARGET_DIR="/home/user/Projects/Euphoria/lands/urion.uz"
          mkdir -p $TARGET_DIR
          
          # Build papkasini aniqlash (out yoki .next)
          if [ -d "out" ]; then
            SOURCE_DIR="out"
          elif [ -d ".next" ]; then
            SOURCE_DIR=".next"
          else
            echo "❌ Build papkasi topilmadi!"
            exit 1
          fi
          
          echo "Deploying from $SOURCE_DIR to $TARGET_DIR..."
          rm -rf $TARGET_DIR/*
          cp -r $SOURCE_DIR/* $TARGET_DIR/
          echo "✅ Urion muvaffaqiyatli deploy qilindi!"
